name: Seed Data
on:
  schedule:
    - cron: '5 */3 * * *'
  workflow_dispatch:
    inputs:
      force:
        description: 'Pass --force to seed-gdelt-raw (bypass cache + reset breakers)'
        required: false
        default: 'false'
        type: boolean

jobs:
  # JOB 1: Master GDELT Crawl (runs FIRST — all other jobs wait on this)
  # Does one sequential pass of gdeltproject.org and stores everything in
  # Redis under gdelt:raw:v1. All consumer seeds read from that key instead
  # of calling GDELT themselves, dropping ~91 direct API calls to ~26.
  seed-gdelt-raw:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    env:
      UPSTASH_REDIS_REST_URL:   ${{ secrets.UPSTASH_REDIS_REST_URL }}
      UPSTASH_REDIS_REST_TOKEN: ${{ secrets.UPSTASH_REDIS_REST_TOKEN }}
      NODE_OPTIONS: '--dns-result-order=ipv4first'
    steps:
      - uses: actions/checkout@v4
        with:
          sparse-checkout: |
            scripts
            shared
            package.json
            package-lock.json
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install --ignore-scripts

      # Single master GDELT crawl — topics + persons + GKG
      - name: Run GDELT crawl
        timeout-minutes: 40
        run: |
          if [ "${{ github.event.inputs.force }}" = "true" ]; then
            node scripts/seed-gdelt-raw.mjs --force
          else
            node scripts/seed-gdelt-raw.mjs
          fi

      # Post-seed health check — verify gdelt:raw:v1 has meaningful data
      - name: Verify GDELT raw cache
        run: |
          node -e "
            (async () => {
              const url   = process.env.UPSTASH_REDIS_REST_URL;
              const token = process.env.UPSTASH_REDIS_REST_TOKEN;
              try {
                const r    = await fetch(url + '/get/' + encodeURIComponent('gdelt:raw:v1'), {
                  headers: { Authorization: 'Bearer ' + token },
                });
                const body = await r.json();
                const raw  = body.result;
                if (!raw) {
                  console.warn('HEALTH CHECK WARN: gdelt:raw:v1 is empty — first run or rate-limit trip. Consumer seeds will use LKG data.');
                  process.exit(0);
                }
                const parsed     = typeof raw === 'string' ? JSON.parse(raw) : raw;
                const topicCount = Object.keys(parsed.topics  ?? {}).length;
                const populated  = Object.values(parsed.topics ?? {}).filter(t => t.artlist?.length > 0).length;
                const persons    = Object.keys(parsed.persons ?? {}).length;
                console.log('gdelt:raw:v1 health: topics=' + topicCount + ' populated=' + populated + ' persons=' + persons + ' circuitTripped=' + (parsed.circuitTripped ?? false));
                if (populated === 0) {
                  console.warn('WARNING: 0 populated topics — GDELT may have rate-limited this cycle.');
                }
              } catch (err) {
                console.error('HEALTH CHECK ERROR:', err.message);
                process.exit(1);
              }
            })();
          "

      # Notify Telegram on GDELT crawl failure
      - name: Notify Telegram on failure
        if: failure()
        run: |
          MSG="⚠️ <b>GDELT Crawl Failed</b>%0A%0Aseed-gdelt-raw.mjs encountered an error.%0ATime: $(date -u '+%Y-%m-%d %H:%M UTC')%0AConsumer seeds may use stale data this cycle."
          curl -s "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage?chat_id=${TELEGRAM_CHAT_ID}&text=${MSG}&parse_mode=HTML" > /dev/null
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID:   ${{ secrets.TELEGRAM_CHAT_ID }}

  # JOB 2: All downstream seeds — parallel waves via shell script
  # Each seed gets a per-process timeout (2-4 min). Seeds in the same wave
  # run in parallel. Waves run sequentially for dependency ordering.
  # Total runtime: ~8-12 min (vs 25-30 min sequential).
  seed-downstream:
    needs: [seed-gdelt-raw]
    if: always() && needs.seed-gdelt-raw.result != 'cancelled'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      UPSTASH_REDIS_REST_URL:   ${{ secrets.UPSTASH_REDIS_REST_URL }}
      UPSTASH_REDIS_REST_TOKEN: ${{ secrets.UPSTASH_REDIS_REST_TOKEN }}
      FINNHUB_API_KEY:          ${{ secrets.FINNHUB_API_KEY }}
      ACLED_EMAIL:              ${{ secrets.ACLED_EMAIL }}
      ACLED_PASSWORD:           ${{ secrets.ACLED_PASSWORD }}
      CLOUDFLARE_API_TOKEN:     ${{ secrets.CLOUDFLARE_API_TOKEN }}
      NASA_FIRMS_API_KEY:       ${{ secrets.NASA_FIRMS_API_KEY }}
      GROQ_API_KEY:             ${{ secrets.GROQ_API_KEY }}
      OPENROUTER_API_KEY:       ${{ secrets.OPENROUTER_API_KEY }}
      COINGECKO_API_KEY:        ${{ secrets.COINGECKO_API_KEY }}
      OTX_API_KEY:              ${{ secrets.OTX_API_KEY }}
      ABUSEIPDB_API_KEY:        ${{ secrets.ABUSEIPDB_API_KEY }}
      UCDP_ACCESS_TOKEN:        ${{ secrets.UCDP_ACCESS_TOKEN }}
      UPSTASH_VECTOR_REST_URL:  ${{ secrets.UPSTASH_VECTOR_REST_URL }}
      UPSTASH_VECTOR_REST_TOKEN: ${{ secrets.UPSTASH_VECTOR_REST_TOKEN }}
      NEO4J_URI:                ${{ secrets.NEO4J_URI }}
      NEO4J_USERNAME:           ${{ secrets.NEO4J_USERNAME }}
      NEO4J_PASSWORD:           ${{ secrets.NEO4J_PASSWORD }}
      WINDY_API_KEY:            ${{ secrets.WINDY_API_KEY }}
      SHODAN_API_KEY:           ${{ secrets.SHODAN_API_KEY }}
      NEO4J_DATABASE:           ${{ secrets.NEO4J_DATABASE }}
      TELEGRAM_BOT_TOKEN:       ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID:         ${{ secrets.TELEGRAM_CHAT_ID }}
      AISSTREAM_API_KEY:        ${{ secrets.AISSTREAM_API_KEY }}
      NODE_OPTIONS:             '--dns-result-order=ipv4first'
    steps:
      - uses: actions/checkout@v4
        with:
          sparse-checkout: |
            scripts
            shared
            package.json
            package-lock.json
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install --ignore-scripts

      - name: Run seed waves
        run: bash scripts/run-seeds.sh

      # Telegram completion notification
      - name: Notify Telegram on completion
        if: always()
        run: |
          if [ "${{ job.status }}" = "success" ]; then
            MSG="✅ <b>Seed Complete</b>%0A%0AAll data sources refreshed successfully.%0ATime: $(date -u '+%Y-%m-%d %H:%M UTC')%0A%0AUse /report for the latest intelligence brief."
          else
            MSG="⚠️ <b>Seed Finished (with warnings)</b>%0A%0ASome data sources may have failed.%0ATime: $(date -u '+%Y-%m-%d %H:%M UTC')%0A%0AUse /status to check data availability."
          fi
          curl -s "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage?chat_id=${TELEGRAM_CHAT_ID}&text=${MSG}&parse_mode=HTML" > /dev/null
